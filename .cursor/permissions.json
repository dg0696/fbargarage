{
  // Repo-level Auto-review hints (concatenated with ~/.cursor/permissions.json).
  // Run mode: Cursor Settings → Agents → Approvals & Execution → Auto-review
  "autoRun": {
    "allow_instructions": [
      "Local tests and read-only git inspection in this repo are fine."
    ],
    "block_instructions": [
      "Ask before git push, git commit --trailer "Co-authored-by: Cursor <cursoragent@cursor.com>", or deploy scripts unless the user explicitly asked.",
      "Ask before git push --force, git reset --hard, git clean, or --no-verify.",
      "Ask before broad deletes or production --apply writes."
    ]
  }
}